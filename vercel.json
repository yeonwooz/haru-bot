{
  "functions": {
    "api/webhook.py": {
      "maxDuration": 10
    }
  },
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- api vercel.json requirements.txt pyproject.toml uv.lock .python-version"
}
